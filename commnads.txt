1. Po stworzeniu plików, uczeń wpisuje w terminalu: docker-compose up -d
2. W terminalu (będąc w folderze projektu) nadaj mu uprawnienia zapisu dla serwera: chmod 777 uploads

uruchomienie: docker-compose up -d
zamkniecie kontener: docker-compose down --volumes --remove-orphans
przebudowanie kontenera: docker-compose up --build -d

http://localhost:8080/index.php
http://localhost:8081/index.php?route=/&db=lab_db